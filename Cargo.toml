[package]
name = "ark-sponge"
version = "0.2.0"
authors = []
edition = "2018"

[profile.release]
opt-level = 3
lto = "thin"
incremental = true
panic = 'abort'

[dependencies]
ark-ff = { version = "0.2.0", default_features = false }

[features]
default = ["std"]
std = [ "ark-ff/std" ]
